package helpers

/*
func TestTrimRuns(t *testing.T) {

	runs := 0

	posCases := map[string]string{
		"":           "",
		"47hlkjhkgj": "47",
		"1":          "1",
		"2n1":        "2",
		"lskdfj":     "",
	}

	for trimmed, expected := range posCases {
		runs++
		if trimRuns(trimmed) != expected {
			t.Errorf("expected %q but got %q", expected, trimmed)
		}
	}

	negCases := map[string]string{
		"":     "a",
		"a":    "a",
		"aa12": "12",
	}

	for trimmed, expected := range negCases {
		runs++
		if trimRuns(trimmed) == expected {
			t.Errorf("expected %q but got %q", expected, trimmed)
		}
	}

	fmt.Println("Runs: ", runs)

}
*/
